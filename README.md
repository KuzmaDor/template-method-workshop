Small workshop to see how the `template method` design pattern works. I wanted to make it more fun by adding "character voicelines" so i asked copilot to add in-character console logs, but the code is written manually 


sidenote: the balancing is all over the place, change it later. orcs win every time and it's not close.
